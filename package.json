{
  "name": "personalsitereacted",
  "version": "1.0.0",
  "description": "This is my personal site recreated using React for the front-end",
  "main": "./server/App.jsx",
  "scripts": {
    "start": "node server/App.js",
    "build": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "23.11.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "nodemon": "^3.1.10",
    "path": "^0.12.7"
  }
}
